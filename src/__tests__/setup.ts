/**
 * Jest テストセットアップファイル
 * 全テストの実行前に読み込まれる共通設定
 */

// グローバルなテストタイムアウトを設定（デフォルト: 5秒）
jest.setTimeout(10000);

// モック関数のデフォルト動作設定
beforeEach(() => {
  // 各テストの前にモックをリセット
  jest.clearAllMocks();
});

afterEach(() => {
  // テスト後のクリーンアップ
});

// グローバルなテストヘルパー関数があればここに追加
